{
  "extends": "@react-native/typescript-config",
  "compilerOptions": {
    // Base config lists "jest" here, but this project has no test suite.
    "types": []
  },
  "include": ["**/*.ts", "**/*.tsx"],
  "exclude": ["**/node_modules", "**/Pods"]
}
